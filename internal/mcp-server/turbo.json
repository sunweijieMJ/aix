{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**", "data/**"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "../../tsdown.config.ts",
        "../../packages/*/package.json",
        "../../packages/*/README.md",
        "../../packages/*/src/**"
      ]
    }
  }
}
