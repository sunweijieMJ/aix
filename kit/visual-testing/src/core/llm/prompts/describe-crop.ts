/**
 * 对「无法用结构化差异解释的区域」做一句话视觉描述
 *
 * 只描述看到的差异，不评分、不给修复代码。输出 JSON 便于解析。
 */

export const DESCRIBE_CROP_PROMPT = `
你将看到同一 UI 区域的两张局部裁图：第一张来自 Figma 设计稿，第二张来自浏览器实际渲染。
该区域的位置、尺寸、颜色、字体等结构化属性已经比对过且均在容差内，因此差异很可能在图标形状、图片内容、插画细节或渲染方式上。

区域相关的设计节点：{{nodes}}

请用一句中文描述两张图最主要的视觉差异；如果看不出实质差异，写 "无明显差异"。
只输出 JSON：{"note": "<一句话>"}
`;
