/**
 * @typedef {import('@/utils/override').CustomLayoutConfig} CustomLayoutConfig
 */

/**
 * <%= it.project %> - Layout 覆盖
 *
 * 支持能力：
 * - layout: 替换整个 Layout 组件
 * - components: 替换 Layout 内部区域（header / menu / main）
 * - defaultConfig: 覆盖默认配置值
 *
 * 注意：layout 与 components 二选一，设置 layout 后 components 无效
 *
 * @returns {CustomLayoutConfig}
 */
export function getCustomLayout() {
  return {
    // components: {},
    // defaultConfig: {},
  };
}
