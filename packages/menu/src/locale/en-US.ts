import type { MenuLocale } from './types';

const enUS: MenuLocale = {
  resizeHandle: 'Drag to resize the menu',
  searchPlaceholder: 'Search',
  noResults: 'No matching items',
  searchClear: 'Clear search keyword',
};

export default enUS;
