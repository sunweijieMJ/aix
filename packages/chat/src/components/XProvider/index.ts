/**
 * @fileoverview XProvider 组件导出
 */

import XProvider from './index.vue';

export default XProvider;
export { X_PROVIDER_KEY } from './types';
export type {
  XProviderProps,
  XProviderConfig,
  XComponentStyleConfig,
  XBubbleConfig,
  XSenderConfig,
  XConversationsConfig,
  XComponentsConfig,
} from './types';
